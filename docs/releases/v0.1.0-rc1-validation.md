# v0.1.0-rc1 Validation

Validation date: 2026-05-03

Release-candidate version labels:

- General Linux artifacts: `0.1.0-rc1`
- Debian package version: `0.1.0+rc1`
- RPM package version: `0.1.0_rc1-1.fc44`
- Arch package version: `0.1.0_rc1-1`
- Alpine package version: `0.1.0_rc1-r0`

Built artifact set under local `dist/release`:

- `pwned-check_0.1.0-rc1_linux_amd64.tar.gz`
- `pwned-check_0.1.0-rc1_linux_arm64.tar.gz`
- `pwned-check-native-pam_0.1.0+rc1_amd64.deb`
- `pwned-check-native-pam-0.1.0_rc1-1.fc44.x86_64.rpm`
- `pwned-check-native-pam-0.1.0_rc1-1-x86_64.pkg.tar.zst`
- `pwned-check-native-pam-0.1.0_rc1-r0.apk`
- per-artifact SHA256 files and build metadata
- `native-pam-SHA256SUMS.txt`
- `native-pam-provenance.json`

Smoke validation completed:

- Debian VM: exact `.deb` installed, exercised through the Debian/Ubuntu native package smoke, disabled, removed, and cleaned up.
- Fedora VM: exact RPM installed, exercised through authselect enablement, rolled back, removed, and cleaned up.
- Arch Docker: exact pacman package installed, exercised through manual PAM helper allow/reject cases, rolled back, removed, cleaned up, and exported locally.
- Alpine VM: exact APK installed from a package built with a prebuilt Go 1.26 checker, exercised through manual PAM helper allow/reject cases, rolled back, removed, and cleaned up.
- Docker helper package smoke: exact `0.1.0-rc1` Linux package path passed across Debian, Ubuntu, Fedora, Rocky Linux, Arch Linux, and Alpine.

Fixes made during RC packaging:

- Alpine package version normalization now preserves `_rc` prerelease notation instead of producing an invalid dotted prerelease version.
- Alpine `abuild` output is isolated to the package script's temporary work directory so same-version rebuilds cannot copy stale packages from the user's package cache.
- Alpine package signing keys are trusted through `sudo` when building as a non-root VM user.
- Alpine Linux-PAM package/module placement includes both `/usr/lib/security` and `/lib/security`, matching the loader paths observed across the persistent VM and pinned Docker image.
- Manual installed smoke cleanup now removes root-owned rollback backups through the existing privilege helper.
- Arch and Alpine package smoke scripts accept version overrides for exact release-candidate package validation.
