# v0.3.0 Production Release Gate Evidence

Evidence captured on 2026-05-06 for
`3c24cf5ce195a4c46f8f767264441e2c83d11500` and signed tag `v0.3.0`.

Gate checklist source: [Production release gate](../production-release-gate.md).

## Summary

| Area | Status | Evidence |
|---|---|---|
| Signing | Pass | Signed tag `v0.3.0` verified locally with OpenPGP fingerprint `BDF6 F4DD 343E 9F10 EA9D B510 FDDA 2848 A95A D641`; release workflow published checksums, build metadata, native PAM provenance, and package assets. |
| Repository publication | Pass | GitHub Pages repository endpoints are published for apt, RPM, Arch, and Alpine. `make native-pam-repo-endpoint-check` passed on 2026-05-06. |
| Smoke validation | Pass | Local pre-push package smokes, tag workflow Docker smokes, endpoint checks, and the live repository VM matrix passed. |
| Rollback validation | Pass | Live repository smokes exercised install, dry-run enablement, enforcement, disable, package removal, rollback, and managed-file cleanup for each published repository family and architecture. |
| Provider outage behavior | Pass | `make smoke`, `make native-pam-harness`, and native PAM VM smokes cover mocked fail-open/fail-closed provider behavior. Operator outage posture is documented in `docs/linux-install.md`, `docs/provider-policy.md`, and `docs/security-model.md`. |
| Documentation | Pass | New operators start with `docs/linux-install.md`; repository internals and key rotation are in `docs/package-repositories.md`; design and security details are in the focused maintainer docs linked from `docs/README.md`. |
| Board closeout | In progress | EP11-S1, EP11-S2, and EP11-S3 are closed. EP11-S5 and EP11-S6 remain open before Epic 11 can close. |

Production readiness is not declared by this document alone. The final
declaration is made by EP11-S6 after board closeout confirms that the evidence,
documentation, and release posture agree.

## Release Evidence

- GitHub Release: <https://github.com/phillipmcmahon/pwned-check/releases/tag/v0.3.0>
- Main CI before tagging: <https://github.com/phillipmcmahon/pwned-check/actions/runs/25433454780>
- Tag-triggered release workflow: <https://github.com/phillipmcmahon/pwned-check/actions/runs/25433743282>
- Release commit: `3c24cf5ce195a4c46f8f767264441e2c83d11500`
- Release tag: signed `v0.3.0`
- Release assets: 32 assets downloaded from the immutable GitHub Release.
- Checksum verification: `sha256sum -c SHA256SUMS.txt` and
  `sha256sum -c native-pam-SHA256SUMS.txt` both passed.
- NAS archive: release artifacts archived to
  `homestorage:/volume1/homes/phillipmcmahon/code/pwned-check/archive/v0.3.0`
  and
  `homestorage:/volume1/homes/phillipmcmahon/code/pwned-check/latest/v0.3.0`.

## Endpoint Evidence

Command:

```bash
make native-pam-repo-endpoint-check
```

Result:

```text
apt endpoint passed
rpm endpoint passed
arch endpoint passed
alpine endpoint passed
Native PAM repository endpoint check passed
```

The endpoint monitor validates public keys, signed metadata, and package
visibility without installing packages on target hosts.

## Live Repository Install Smoke Evidence

Command:

```bash
make native-pam-live-repo-smokes
```

Result:

```text
Native PAM live repository smoke gate complete
```

Final output:

```text
.test-output/native-pam-live-repo-smokes/20260506T123248Z-native-pam-live-repo-smokes/20260506T123248Z-native-pam-live-repo-smokes.txt
```

The final pass installed v0.3.0 from the public repository endpoints and
removed it again on:

| Family | Hosts | Published version checked |
|---|---|---|
| Apt | `codex-vm-ubuntu`, `codex-vm-debian`, `codex-vm-ubuntu-arm64`, `codex-vm-debian-arm64` | `0.3.0` |
| RPM | `codex-vm-fedora`, `codex-vm-fedora-arm64`, `codex-vm-rocky`, `codex-vm-rocky-arm64` | `0.3.0-1.fc44` |
| Arch | `codex-vm-arch` | `0.3.0-1` |
| Alpine | `codex-vm-alpine`, `codex-vm-alpine-arm64` | `0.3.0-r0` |

The first live repository smoke run found stale `0.1.6` repository metadata.
The repositories were regenerated from the immutable v0.3.0 GitHub Release
package assets and published on `gh-pages` before the final pass. The smoke
scripts now assert the installed `/usr/bin/pwned-check --version` so a stale
repository cannot pass this gate.

## Open Gate Items

The remaining production-gate work is intentionally narrow:

- EP11-S6 records the final Linux production readiness declaration after the
  board state is clean.
