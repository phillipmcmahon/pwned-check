# v0.3.0 Production Release Gate Evidence

Evidence captured on 2026-05-06 for
`3c24cf5ce195a4c46f8f767264441e2c83d11500` and signed tag `v0.3.0`.

Gate checklist source: [Production release gate](../production-release-gate.md).

## Summary

| Area | Status | Evidence |
|---|---|---|
| Signing | Pass | Signed tag `v0.3.0` verified locally with OpenPGP fingerprint `BDF6 F4DD 343E 9F10 EA9D B510 FDDA 2848 A95A D641`; release workflow published checksums, build metadata, native PAM provenance, and package assets. |
| Repository publication | Pass | GitHub Pages repository endpoints are published for apt, RPM, Arch, and Alpine. `make native-pam-repo-endpoint-check` passed on 2026-05-06. |
| Smoke validation | In progress | Local pre-push package smokes and tag workflow Docker smokes passed. Repository-only mutating VM install smokes are tracked in EP11-S5 before production readiness declaration. |
| Rollback validation | In progress | Package and persistent-VM smokes in the pre-push gate validate dry-run, enforcement, disable, removal, and rollback. Live repository rollback evidence is tracked in EP11-S5. |
| Provider outage behavior | Pass | `make smoke`, `make native-pam-harness`, and native PAM VM smokes cover mocked fail-open/fail-closed provider behavior. Operator outage posture is documented in `docs/operations.md`, `docs/provider-policy.md`, and `docs/security-model.md`. |
| Documentation | Pass | New operators start with `docs/linux-install.md`; operator recovery is in `docs/operations.md`; repository internals and key rotation are in `docs/package-repositories.md`; troubleshooting is in `docs/troubleshooting.md`. |
| Board closeout | In progress | EP11-S1, EP11-S2, and EP11-S3 are closed. EP11-S5 and EP11-S6 remain open before Epic 11 can close. |

Production readiness is not declared by this document alone. The final
declaration is made by EP11-S6 after EP11-S5 records the full live repository
install smoke matrix.

## Release Evidence

- GitHub Release: <https://github.com/phillipmcmahon/pwned-check/releases/tag/v0.3.0>
- Main CI before tagging: <https://github.com/phillipmcmahon/pwned-check/actions/runs/25433454780>
- Tag-triggered release workflow: <https://github.com/phillipmcmahon/pwned-check/actions/runs/25433743282>
- Release commit: `3c24cf5ce195a4c46f8f767264441e2c83d11500`
- Release tag: signed `v0.3.0`
- Release assets: 32 assets downloaded from the immutable GitHub Release.
- Checksum verification: `sha256sum -c SHA256SUMS.txt` and
  `sha256sum -c native-pam-SHA256SUMS.txt` both passed.
- NAS archive: release artifacts archived to
  `homestorage:/volume1/homes/phillipmcmahon/code/pwned-check/archive/v0.3.0`
  and
  `homestorage:/volume1/homes/phillipmcmahon/code/pwned-check/latest/v0.3.0`.

## Endpoint Evidence

Command:

```bash
make native-pam-repo-endpoint-check
```

Result:

```text
apt endpoint passed
rpm endpoint passed
arch endpoint passed
alpine endpoint passed
Native PAM repository endpoint check passed
```

The endpoint monitor validates public keys, signed metadata, and package
visibility without installing packages on target hosts.

## Open Gate Items

The remaining production-gate work is intentionally narrow:

- EP11-S5 records the mutating live repository install smoke matrix across the
  persistent distro VMs.
- EP11-S6 records the final Linux production readiness declaration after the
  live smoke matrix passes and board state is clean.
